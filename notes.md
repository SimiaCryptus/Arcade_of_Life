- BUG: custom levels appear to be unwinnable. I've destroyed all enemy bases, nothing happens!
- update max base zone height when the player draw zone % has been changed
- vertical offset wrapping needs to be configurable from the level designer settings ui (it is missing)
- the level designer should be able to enable/disable each event detection type (e.g. Ricochet)
- Add pretty announcements when each wave is started

- BUG: custom levels appear to be unwinnable. I've destroyed all enemy bases, nothing happens!
- bug: level designer shows no event types in the configuration panel - the section is there but empty; the level designer should be able to enable/disable each event detection type (e.g. Ricochet)
- The level designer should control the default starting speed for the game (the game should still always start paused)
- the wrap vertical offset feature does not work - appears to do nothing.
- the first enabled tool should be automatically selected at the start of the game
- the first enabled pattern should be automatically selected at the start of the game




- BUG: custom levels appear to be unwinnable. I've destroyed all enemy bases, nothing happens!
- level designer's default/starting speed setting is not working - the game always starts at the same speed regardless of the setting
- slider ranged in the level designer should not actually go up to 99999; that is a special value that means "infinite". To keep the slider usable, the max should be reasonable
- wrap vertical offset should be reversed - currently it wraps on the other side and must be entered in as negative to workaround the issue
- when playing a custom level, and losing, pretting play again should replay that custom level. it currently goes to the default game mode.

