- better death rule: when expiring cells, we want to increment the age of each cell neighbor by a configurable amount, and then remove any cells that are above the age threshold. This will prevent loss-tolerant patterns from persisting indefinitely
- the ink meter should have a number displayed on top of the colored fill
- when using the "exit" to main menu button, any `level` parameter in the url should be cleared, so that the user doesn't accidentally reload the level they were just playing
- add a "restart level" button to the game, with confirmation
- in hex mode, annihilation visuals are shown in the wrong location
- level designer colors settings do not effect the actual game

- pattern
